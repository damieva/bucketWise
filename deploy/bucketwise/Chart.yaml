apiVersion: v2
name: bucketwise
description: Bucketwise service
type: application
version: 0.1.0
appVersion: "1.0.0"

dependencies:
  - name: mongodb
    version: "0.12.3"
    repository: "oci://registry-1.docker.io/cloudpirates"
    condition: mongodb.enabled
  - name: ollama
    version: "1.54.0"
    repository: "https://helm.otwld.com/"
    condition: ollama.enabled
