apiVersion: v1
kind: Service
metadata:
  name: {{ include "bucketwise.fullname" . }}
  labels:
    {{- include "bucketwise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bucketwise.selectorLabels" . | nindent 4 }}
