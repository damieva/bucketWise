apiVersion: v1
kind: Service
metadata:
  name: {{ include "bucketwise-backend.fullname" . }}
  labels:
    {{- include "bucketwise-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bucketwise-backend.selectorLabels" . | nindent 4 }}
